# Disable FTS completely (bypass the sed error)
# This file explicitly sets mail plugins without the FTS plugin
# to avoid the problematic sed command in the initialization script

mail_plugins = $mail_plugins quota imap_quota notify push_notification